[package]
name = "hk"
version = "1.0.0"
edition = "2021"
description = "Safe idiomatic Rust bindings for the HK neural tensor format and SIMD compute engine."
license = "Apache-2.0"


[dependencies]

